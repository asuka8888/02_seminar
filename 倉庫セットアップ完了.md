# 📦 MCP Servers 倉庫セットアップ完了

## ✅ 作成されたファイル

### 1. セットアップスクリプト
**場所:** `~/mcp-servers-setup.sh`

**用途:** Facebook MCP Serverを倉庫に配置する初回セットアップ

**実行方法:**
```bash
bash ~/mcp-servers-setup.sh
```

---

### 2. クイック起動スクリプト
**場所:** `~/mcp-servers-run-facebook.sh`

**用途:** Facebook MCP Serverをワンコマンドで起動

**実行方法:**
```bash
bash ~/mcp-servers-run-facebook.sh
```

---

### 3. 管理ガイド
**場所:** `~/MCP-SERVERS-README.md`

**内容:**
- 倉庫の使い方
- トラブルシューティング
- セキュリティ注意事項

**閲覧方法:**
```bash
cat ~/MCP-SERVERS-README.md
```

---

## 🚀 次のステップ（3ステップで完了）

### ステップ1: セットアップを実行

ターミナルを開いて以下を実行:

```bash
bash ~/mcp-servers-setup.sh
```

これにより以下が自動実行されます:
- ✅ `~/mcp-servers/` ディレクトリ作成
- ✅ Facebook MCP Serverのクローン
- ✅ Python 3.11環境のセットアップ
- ✅ 依存関係のインストール
- ✅ .envファイルのテンプレート作成

---

### ステップ2: Facebook認証情報を設定

#### 2-1. アクセストークンの取得

1. [Facebook Graph API Explorer](https://developers.facebook.com/tools/explorer) を開く
2. 右上「User Token」→「**Page Access Token**」に変更
3. 管理するページを選択
4. 以下の権限を追加:
   - ✅ `pages_manage_posts`
   - ✅ `pages_read_engagement`
   - ✅ `pages_manage_metadata`
   - ✅ `pages_read_user_content`
   - ✅ `pages_messaging`
5. 「Generate Access Token」をクリック
6. 生成されたトークンをコピー

#### 2-2. Page IDの取得

- [FindMyFBID](https://findmyfbid.com/) にページURLを入力
- または、ページ設定 → ページ情報 から確認

#### 2-3. .envファイルに設定

```bash
nano ~/mcp-servers/facebook-mcp-server/.env
```

以下のように編集:
```env
FACEBOOK_ACCESS_TOKEN=EAABsbCS...（コピーしたトークン）
FACEBOOK_PAGE_ID=123456789...（取得したPage ID）
```

保存: `Ctrl + O` → Enter → `Ctrl + X`

---

### ステップ3: 接続テストと起動

#### テストの実行

```bash
cd ~/mcp-servers/facebook-mcp-server
source .venv/bin/activate
python test_connection.py
```

**成功時の表示:**
```
✅ アクセストークン有効
✅ ページアクセス成功
✅ すべての必要な権限が付与されています
🎉 すべてのテストに合格しました！
```

#### サーバーの起動

```bash
# 方法1: クイック起動（推奨）
bash ~/mcp-servers-run-facebook.sh

# 方法2: 直接実行
cd ~/mcp-servers/facebook-mcp-server
source .venv/bin/activate
python server.py
```

---

## 💡 Claude Codeでの使用例

サーバー起動後、Claude Codeで以下のようなタスクが実行できます:

### 例1: 投稿の作成
```
ユーザー: Facebookページに「本日のキャンペーン情報」を投稿して

Claude: post_to_facebook ツールを使用して投稿を作成します...
✅ 投稿が作成されました！
```

### 例2: インサイト分析
```
ユーザー: 最新投稿のパフォーマンスを分析して

Claude: インサイトを取得します...
📊 インプレッション: 1,234回
💙 いいね: 89件
💬 コメント: 12件
```

### 例3: コメント管理
```
ユーザー: ネガティブなコメントを非表示にして

Claude: filter_negative_comments を実行します...
✅ 3件のコメントを非表示にしました
```

---

## 📋 利用可能な機能（28ツール）

| カテゴリ | ツール数 | 主な機能 |
|---------|---------|----------|
| 📝 投稿管理 | 6 | 作成・編集・削除・スケジュール |
| 💬 コメント管理 | 10 | 返信・削除・非表示・フィルタリング |
| 📊 インサイト | 11 | インプレッション・エンゲージメント分析 |
| 📨 メッセージ | 1 | DM送信 |

詳細は `~/mcp-servers/facebook-mcp-server/README_ja.md` を参照

---

## 📁 倉庫の構造

```
~/mcp-servers/                    # MCP専用倉庫（今後拡張可能）
└── facebook-mcp-server/          # Facebook MCP Server
    ├── server.py                 # MCPサーバー本体
    ├── .env                      # 認証情報（要設定）
    ├── .venv/                    # Python仮想環境
    ├── README_ja.md              # 詳細ガイド
    ├── test_connection.py        # 接続テスト
    └── SETUP_GUIDE.md            # セットアップガイド
```

---

## 🔧 よくある問題と解決策

### Q1: セットアップスクリプトが実行できない

**解決策:**
```bash
# 実行権限を付与
chmod +x ~/mcp-servers-setup.sh
chmod +x ~/mcp-servers-run-facebook.sh

# 再実行
bash ~/mcp-servers-setup.sh
```

### Q2: "Invalid access token" エラー

**原因:** トークンが無効または期限切れ

**解決策:**
1. Graph API Explorerで新しいトークンを生成
2. .envファイルを更新
3. サーバーを再起動

### Q3: Python バージョンエラー

**確認:**
```bash
~/mcp-servers/facebook-mcp-server/.venv/bin/python --version
# Python 3.11.14 と表示されるべき
```

**解決策:**
```bash
cd ~/mcp-servers/facebook-mcp-server
~/.local/bin/uv venv --python 3.11
~/.local/bin/uv pip install -r requirements.txt
```

---

## 📚 サポート資料

- **管理ガイド:** `~/MCP-SERVERS-README.md`
- **詳細マニュアル:** `~/mcp-servers/facebook-mcp-server/README_ja.md`
- **セットアップガイド:** `~/mcp-servers/facebook-mcp-server/SETUP_GUIDE.md`

---

## 🎯 まとめ

✅ **完了したこと:**
- MCP倉庫管理システムの構築
- Facebook MCP Server用セットアップスクリプト作成
- クイック起動スクリプト作成
- 詳細ドキュメント作成

⏭️ **次にやること:**
1. `bash ~/mcp-servers-setup.sh` を実行
2. Facebook認証情報を設定
3. 接続テストを実行
4. Claude Codeで活用開始！

---

**作成日:** 2026年1月5日
**バージョン:** 1.0

Facebook MCP Serverを「倉庫」に安全に保管し、
いつでも呼び出せる準備が整いました！ 🎉
